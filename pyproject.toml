[project]
name = "pub-oembed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Uchenna Orji", email = "bethuelorji@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "requests>=2.34.2",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
